-- ============================================================
-- (의도적 no-op)
--
-- 이 파일은 002_notifications.sql과 바이트 단위로 동일한 중복이었다.
-- 커밋 8af2950이 bca39db가 이미 넣은 002를 003으로 다시 추가한 실수다.
-- CREATE POLICY는 IF NOT EXISTS가 없어서, 001부터 새 DB에 재생하면
--   ERROR: policy "notifications_select_own" ... already exists (42710)
-- 로 003에서 멈춘다. 즉 마이그레이션 체인이 재생 불가능했다.
--
-- 파일을 지우지 않고 비워 두는 이유: 운영 DB의 마이그레이션 이력에
-- 003이 기록돼 있을 수 있어, 파일을 삭제하면 db push 시 이력이 어긋난다.
-- 내용은 002가 전부 담고 있다.
-- ============================================================
